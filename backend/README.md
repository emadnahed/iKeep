# iKeep Backend

This is the backend for the iKeep application. It is a Node.js application using the Express framework and MongoDB for the database.

## Running the service

To run the backend service, you can use the `docker-compose.yml` file in the root directory.

```bash
docker-compose up -d backend
```

This will start the backend service on port 5000.
